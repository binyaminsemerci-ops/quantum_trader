# Backend environment example
# Copy to backend/.env and fill in real values (do NOT commit real secrets)

DB_USER=your_db_username
DB_PASS=your_db_password
# Example connection string (use env interpolation locally or your tooling):
# postgresql://<DB_USER>:<DB_PASS>@localhost:5432/quantum_trader

DATABASE_URL=postgresql://<DB_USER>:<DB_PASS>@localhost:5432/quantum_trader
SECRET_KEY=changeme
SAFETY_API_KEY=
DEFAULT_EXCHANGE=binance
BINANCE_API_KEY=
BINANCE_API_SECRET=
