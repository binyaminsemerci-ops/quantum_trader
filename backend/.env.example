# Backend environment example
# Copy to backend/.env and fill in real values (do NOT commit real secrets)

DATABASE_URL=postgresql://user:password@localhost:5432/quantum_trader
SECRET_KEY=changeme
SAFETY_API_KEY=
DEFAULT_EXCHANGE=binance
BINANCE_API_KEY=
BINANCE_API_SECRET=
