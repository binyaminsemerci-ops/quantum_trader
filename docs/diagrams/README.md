# Quantum Trader VPS Topology

Source: `quantum_trader_vps_topology.mmd`  
Rendered: `quantum_trader_vps_topology.png` (generate from Mermaid source below or use Mermaid CLI)

## Generate PNG
```bash
# Install Mermaid CLI (one-time)
npm install -g @mermaid-js/mermaid-cli

# Render to PNG
mmdc -i quantum_trader_vps_topology.mmd -o quantum_trader_vps_topology.png -b transparent
```

Or paste the .mmd contents into https://mermaid.live to export as PNG.
