# ==============================================================================
# P2.6 Portfolio Heat Gate - Environment Configuration
# ==============================================================================

# Redis connection
REDIS_HOST=localhost
REDIS_PORT=6379

# HTTP ports
HEAT_METRICS_PORT=8068
HEAT_HEALTH_PORT=8069

# Stream configuration
HEAT_STREAM_IN=quantum:stream:harvest.proposal
HEAT_STREAM_OUT=quantum:stream:harvest.heat.decision
HEAT_GROUP=heat_gate
HEAT_CONSUMER=heat_gate_1
HEAT_POLL_MS=500

# State key
HEAT_STATE_KEY=quantum:portfolio:state

# Staleness threshold (seconds)
STALE_SEC=600

# ==============================================================================
# Heat Calculation - Targets (safe defaults)
# ==============================================================================

GROSS_TARGET_USD=5000
DD_TARGET_USD=250
BURST_TARGET_USD=250
FEE_TARGET_USD=50
CHURN_TARGET=1.0

# ==============================================================================
# Heat Calculation - Component Weights
# ==============================================================================

W_EXP=1.0
W_DD=1.0
W_BURST=1.0
W_FEE=0.5
W_CHURN=0.5

# ==============================================================================
# Heat Thresholds and Partials
# ==============================================================================

T_WARM=0.45
T_HOT=0.70
WARM_PARTIAL=0.50
HOT_PARTIAL=0.25

# ==============================================================================
# Hold-Close (shadow-only marker for analytics)
# ==============================================================================

ENABLE_HOLD_CLOSE=false
HOLD_KILL_MAX=0.30
