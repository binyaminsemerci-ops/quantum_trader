# Add this to /etc/prometheus/prometheus.yml under scrape_configs:

  - job_name: 'metricpack_builder'
    static_configs:
      - targets: ['localhost:8051']
        labels:
          service: 'metricpack_builder'
          component: 'observability'
