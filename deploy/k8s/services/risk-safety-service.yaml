apiVersion: v1
kind: Service
metadata:
  name: risk-safety-service
  namespace: quantum-trader
  labels:
    app: risk-safety-service
    component: risk-safety
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: risk-safety-service
