apiVersion: v1
kind: Service
metadata:
  name: federation-ai-service
  namespace: quantum-trader
  labels:
    app: federation-ai-service
    component: federation-ai
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8007
    targetPort: 8007
    protocol: TCP
    name: http
  selector:
    app: federation-ai-service
