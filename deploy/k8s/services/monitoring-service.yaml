apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: quantum-trader
  labels:
    app: monitoring-service
    component: monitoring
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP
    name: http
  selector:
    app: monitoring-service
