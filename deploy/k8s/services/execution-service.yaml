apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: quantum-trader
  labels:
    app: execution-service
    component: execution
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: execution-service
