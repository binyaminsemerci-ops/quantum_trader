apiVersion: v1
kind: Service
metadata:
  name: ai-engine-service
  namespace: quantum-trader
  labels:
    app: ai-engine-service
    component: ai-engine
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: ai-engine-service
