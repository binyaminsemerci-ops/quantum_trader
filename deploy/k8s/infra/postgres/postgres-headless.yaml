apiVersion: v1
kind: Service
metadata:
  name: postgres-ha-headless
  namespace: quantum-trader
  labels:
    app: postgres-ha
    tier: infrastructure
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: postgres-ha
