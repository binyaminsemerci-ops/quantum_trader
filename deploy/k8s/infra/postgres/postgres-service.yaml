apiVersion: v1
kind: Service
metadata:
  name: postgres-ha
  namespace: quantum-trader
  labels:
    app: postgres-ha
    tier: infrastructure
spec:
  type: ClusterIP
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  selector:
    app: postgres-ha
