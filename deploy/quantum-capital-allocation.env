# P2.9 Capital Allocation Brain - Environment Configuration
# Created: 2026-01-28

# Mode: shadow (log only) | enforce (write allocation targets)
P29_MODE=shadow

# Loop interval (seconds)
P29_INTERVAL_SEC=5

# Metrics port
P29_METRICS_PORT=8059

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Safety caps (40% max per symbol, 60% max per cluster)
MAX_SYMBOL_PCT=0.40
MAX_CLUSTER_PCT=0.60

# Stale data threshold (seconds)
STALE_DATA_SEC=60

# Regime factors
REGIME_FACTOR_TREND=1.2
REGIME_FACTOR_MR=1.0
REGIME_FACTOR_CHOP=0.6

# Drawdown thresholds and factors
DD_LOW_THRESHOLD=0.05
DD_HIGH_THRESHOLD=0.15
DD_FACTOR_LOW=1.0
DD_FACTOR_MID=0.7
DD_FACTOR_HIGH=0.4

# Cluster stress clamp range
CLUSTER_STRESS_MIN=0.3
CLUSTER_STRESS_MAX=1.0

# Target hash TTL (seconds)
TARGET_TTL_SEC=300

# Logging
LOG_LEVEL=INFO
