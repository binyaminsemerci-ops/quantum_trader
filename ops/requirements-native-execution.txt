# Native Execution Service Requirements (Python 3.12 compatible)
# Install: /opt/quantum/venvs/execution/bin/pip install -r ops/requirements-native-execution.txt

# Build tooling (Python 3.12 compatibility)
setuptools<70
wheel>=0.42.0
pip>=24.0

# Core dependencies
numpy<2.0  # Python 3.12 compatibility
fastapi==0.109.0
uvicorn[standard]==0.27.0
redis==5.0.1
structlog==24.1.0
python-dotenv==1.0.1
pydantic==2.5.3
pydantic-settings==2.1.0
httpx==0.26.0
aiohttp==3.9.1

# Exchange integration
ccxt==4.2.25
python-binance==1.0.19

# Event loop optimization
uvloop==0.19.0  # Used by uvicorn[standard]

# OpenTelemetry (observability)
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-exporter-otlp==1.22.0

# Logging and utilities
python-json-logger==2.0.7
