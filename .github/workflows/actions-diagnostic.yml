name: Actions Diagnostic

on:
  push:
    branches: [ "main" ]

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Echo
        run: |
          echo "actions diagnostic: jobs scheduled successfully"
